<!-- views/pages/genre_form.ejs -->

<!DOCTYPE html>
<html lang="en">
  <head>
		<title><%= title %></title> 
		<% include ../partials/head %>
  </head>
	<body>
		<div class="container-fluid">
			<div class="row">
				<% include ../partials/sidebar %>
				<div class="col-sm-10">
          <h1><%= title %></h1>
          <form method="POST" action="">
            <div class="form-group"style="margin-right:60%;" >
              <label for="name">Gênero: </label>
              <input placeholder="Fantasy,Poetry etc." class="form-control" 
                id="name" name="name" type="text" 
                value="<%- undefined===genre ? '' : genre.name %>">
              <br>
              <button class="btn btn-primary" type="submit">Cadastrar</button>  
            </div>
          </form>
          <% if(errors.length) { %>  
            <ul>   
            <% for (var i in errors) { %>			
              <li>
                <%= errors[i].msg %>
              </li>           
            <% } %>
            </ul>   
          <% } %>		
				</div>
			</div>
		</div>
	</body>
</html>    